#import cstd.hhh

square: (x: usize) -> usize = {

    #asm
        mov     rax, rdi
        mul     rdi
    #end

    return _;
}

main: -> u32 = {
    a: = 16;
    x: = 0;
    if a == 16 {
        "lol\n";
    } else {
        "lmao\n";
    }
    return 0;
}
