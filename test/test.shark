#include cstd.hhh
#include math.hhh

main: () -> u32 = {
    i: = 0;
    loop {
        printf("%lu\n", i);
        if i {
            continue;
        } else {
            "break\n";
            break;
        }
        inc(&i);
    }
    return 0;
}
