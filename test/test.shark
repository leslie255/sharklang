func main() -> int32 {
    let a: uint64 = 0;
    loop {
        a = uadd(a, 1);
        u64print(a);
        
        if eq(a, 100) {
            jmp      _break_loop
        }
    }
    _break_loop:
    
    return 0;
}
